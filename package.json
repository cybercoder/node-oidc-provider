{
  "name": "@cybercoder/node-oidc-provider",
  "version": "8.4.5",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "esbuild index.ts --platform=node --bundle --format=cjs --outfile=index.js"
  },
  "dependencies": {
    "oidc-provider": "^8.4.5"
  },
  "devDependencies": {
    "esbuild": "^0.20.0"
  }
}
